// app/(app)/debugUserRole.tsx
'use client';
import React from 'react';
type Props = { email: string | null, roles: string[] };
export default function DebugUserRole({ email, roles }: Props) {
  return (
    <div></div>
  );
}
